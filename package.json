{
  "name": "smokingarea-sg",
  "version": "1.0.0",
  "private": true,
  "description": "Every designated smoking area and licensed cigarette retailer in Singapore, on one map.",
  "license": "MIT",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "dev": "node scripts/serve.mjs 5178",
    "data": "node scripts/fetch.mjs && node scripts/geocode.mjs && node scripts/build.mjs && node scripts/check.mjs",
    "data:fetch": "node scripts/fetch.mjs",
    "data:geocode": "node scripts/geocode.mjs",
    "data:build": "node scripts/build.mjs",
    "check": "node scripts/check.mjs"
  }
}
